{% extends template('widget') %}

{% define data = {
    antelope: _widget.antelope
} %}

{% block body %}
    {% include molecule('antelope-box') with {
        data: {
                name: data.antelope.name
            }
    } only %}
    <h1>{{ data.antelope.name }} | {{ data.antelope.color }}</h1>
{% endblock %}